# 画像の切り出し元フォルダを指定する設定ファイルのひな形。
#
# このファイルを同じフォルダに tools/config.local.ps1 という名前でコピーし、
# ご自身の環境のパスに書き換えてください。
# config.local.ps1 は .gitignore に入れてあるので、公開リポジトリには上がりません。
#
# $LP_ROOT には、完成イメージと背景画像が入っている「LP」フォルダを指定します。
#   $LP_ROOT\全体\完成イメージ\  … 各セクションの完成イメージ
#   $LP_ROOT\背景画像\           … ヒーローの海景
$LP_ROOT = "X:\path\to\LP"
